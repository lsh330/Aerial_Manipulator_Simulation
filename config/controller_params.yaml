# ============================================================
# Aerial Manipulator - Controller Parameters
# ============================================================

position_controller:
  type: "pid"
  gains:
    Kp: [6.0, 6.0, 8.0]      # [N/m] proportional gains (x, y, z)
    Kd: [4.5, 4.5, 5.6]      # [N·s/m] derivative gains — z tuned for ζ≈0.7
    Ki: [0.1, 0.1, 0.2]      # [N/(m·s)] integral gains — reduced to avoid underdamping
  integrator_limit: 1.0       # [N] anti-windup saturation

attitude_controller:
  type: "geometric_so3"
  gains:
    Kr: [8.0, 8.0, 4.0]      # [N·m/rad] rotational proportional gains (roll, pitch, yaw)
    Kw: [0.8, 0.8, 0.5]      # [N·m·s/rad] derivative gains — reduced for ζ≈0.7, BW≈2.4Hz

manipulator_controller:
  type: "computed_torque"
  gains:
    Kp: [20.0, 20.0]         # [N·m/rad] joint proportional gains
    Kd: [1.0, 1.0]           # [N·m·s/rad] joint derivative — reduced for ζ≈2 (from 10)
  gravity_compensation: true
  reaction_compensation: true  # compensate reaction torques on quadrotor

control_allocator:
  type: "pseudoinverse"        # mixing matrix pseudoinverse
  motor_saturation: true
